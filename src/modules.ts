export interface Value{
    email:string;
    password:string;
    repPassword:string;
    nip:string;
    phone:string;
    position:string;
}

export interface Valid{
    email:boolean;
    password:boolean;
    repPassword:boolean;
    nip:boolean;
    phone:boolean;    
    position: boolean;
}